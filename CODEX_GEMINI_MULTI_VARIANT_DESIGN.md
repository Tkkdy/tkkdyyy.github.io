# Codex 任务：归档 Gemini 首页 v0.1，并生成三套受控视觉方案

## 任务性质

本轮 Codex 只负责：

1. 归档当前 Gemini 首页原型；
2. 整理第一轮视觉反馈；
3. 在 Gemini 中建立三个彼此独立的设计分支；
4. 将三套结果保存并整理成可比较材料。

本轮仍然不负责：

- 创建正式 Astro 工程；
- 安装 npm 依赖；
- 修改正式网站代码；
- 配置 GitHub Pages；
- 部署；
- 选择正式技术栈；
- 开发后端；
- 提交 Git。

---

# 一、先归档当前原型 v0.1

## 1. 保存材料

在用户指定的项目目录中建立：

```text
design/
└── gemini-home-v0.1/
    ├── desktop.png
    ├── mobile.png
    ├── source/
    ├── GEMINI_NOTES.md
    └── VISUAL_REVIEW.md
```

需要保存：

- 当前 Gemini Canvas 桌面端完整截图；
- 当前 Gemini Canvas 移动端完整截图；
- Gemini 当前可导出的原型代码；
- Gemini 的设计说明；
- Gemini 当前使用的 Design Tokens；
- 本次生成原型时使用的完整提示词；
- Gemini Canvas 或对话的名称和可访问状态。

若无法直接导出某项内容，不要猜测或伪造，只在说明中标记“未能导出”。

不要覆盖用户现有文件。

## 2. 写入第一轮视觉反馈

`VISUAL_REVIEW.md` 至少包含：

```markdown
# Gemini 首页原型 v0.1 视觉验收

## 可以保留

- 桌面端与移动端信息结构基本成立
- 首页栏目完整
- 文章视觉优先、项目入口优先的关系基本成立
- 不需要推倒信息架构
- 页面基础可用性可以接受

## 需要改进

- 当前 AI 模板感较重
- 排版过于规整、同质化
- 缺少作者气质和独特性
- 暖米白、近黑、低饱和绿组合不够高级
- 当前配色略显土、性冷淡、缺乏品味
- 字体大小、字重、层级和留白需要重新调整
- 当前大标题、衬线副标题、胶囊按钮组合较像通用 AI 个人主页
- 精选文章与项目的大卡片加小卡片组合辨识度不足
- 首页顶部导航以后可尝试小面积 Apple 式 Liquid Glass
- Liquid Glass 只用于导航、搜索或少量浮层，不能扩散到主体内容
- 正式网站基础跑通后仍需第二轮视觉精修

## 当前结论

该原型是结构参考和视觉草图，不是最终视觉稿，也不是可直接进入正式工程的生产代码。
```

---

# 二、建立三套独立 Gemini 设计分支

## 总体规则

使用三个独立的 Gemini 对话或 Canvas。

建议命名：

1. `VDVXDV Home A — Refined Editorial`
2. `VDVXDV Home B — Warm Technical`
3. `VDVXDV Home C — Personal Asymmetry`

三套方案都必须：

- 使用同一份最新版 `PRODUCT.md`
- 使用同一份最新版 `CONTENT_MODEL.md`
- 使用同一份最新版 `DESIGN.md`
- 使用同一批参考截图
- 使用同一批真实示例内容
- 保持相同页面结构和功能范围
- 同时生成桌面端和移动端
- 不实现正式后端、搜索、评论、部署或完整网站
- 不加入复杂动效
- 不公开真实姓名
- 不使用网络图库作为正式内容
- 不直接复制参考网站

三套方案的差异必须来自明确的设计方向，而不是随机变化。

不要在同一个 Gemini 对话中依次覆盖三套方案。

---

# 三、方案 A：精致编辑式

## 设计目标

保留当前原型的信息结构，但显著减少 AI 模板感。

关键词：

- 精致
- 编辑式
- 稳定
- 高级
- 温暖
- 克制
- 清楚

## 主要调整

- 保留暖色背景，但不继续使用当前偏灰绿、偏土的组合
- 尝试暖象牙白、深墨色、低饱和酒红或焦糖色作为极少量强调
- 减少标准胶囊按钮
- 减少统一圆角卡片
- 使用细线、字体、留白和不对称比例建立层级
- 重新处理 `VDVXDV` 大标题比例，避免通用 AI Hero
- 提高中文排版品质
- 强化代表文章的编辑视觉
- 项目入口清楚但不做 SaaS 功能卡片
- 导航使用很小面积、很克制的 Apple 式 Liquid Glass

## 不允许

- 蓝紫渐变
- 绿色作为主要强调色
- 大量米灰卡片
- 三列同款卡片
- 通用“主按钮＋次按钮”商业首屏
- 过度极简导致空洞

---

# 四、方案 B：温暖技术感

## 设计目标

建立比当前原型更精确、更现代的技术感，但不能像 Vercel SaaS 官网。

关键词：

- 技术
- 精确
- 温暖
- 黑白结构
- 人文
- 有力量
- 不冰冷

## 主要调整

- 使用暖白与近黑为主
- 强调色只选一种，例如暗红、铜色或深蓝灰
- 更明确的网格与排版张力
- 可以少量采用几何线条、编号、版本信息或等宽字体
- 首屏不使用标准双按钮商业结构
- 文章仍然是首页最重要视觉内容
- 项目展示可以更工程化，但目的先于技术栈
- 导航使用紧凑、半透明的 Liquid Glass 小组件
- 通过字体尺寸和块面比例创造冲击，而不是动效

## 不允许

- 整站纯黑白
- SaaS 营销话术
- 虚假合作 Logo
- 大型几何装饰物
- 过多代码元素
- 冷冰冰的开发者模板

---

# 五、方案 C：个人化非对称

## 设计目标

在保持清楚和高级的前提下，增加作者气质与不完全规整的个人感。

关键词：

- 个人
- 非对称
- 有呼吸感
- 温暖
- 轻微实验性
- 编辑排版
- 不像模板

## 主要调整

- 首页不使用标准居中 Hero
- `VDVXDV`、介绍文字和视觉内容可以采用非对称布局
- 精选文章、项目、随笔和影像使用不同尺寸与排版方式
- 不把所有区域关进卡片
- 使用少量用户摄影占位和自定义图形空间
- 留白可以不完全均匀，但必须有秩序
- 字体层级更具作者感
- 允许一处较明显的低饱和色块，但不增加整体活泼度
- 导航仍然小、清楚，并使用克制 Liquid Glass

## 不允许

- 艺术展览式难以理解
- 隐藏导航
- 大量倾斜、重叠和绝对定位
- 牺牲移动端可读性
- 过度可爱
- 拼贴风
- 无法迁移到正式工程的实验布局

---

# 六、发给每个 Gemini 分支的共同要求

在各分支的专属设计方向之后，追加以下共同要求：

```markdown
请基于已经上传的三份规范文件和全部参考截图，生成一份新的首页视觉原型。

这是一次设计探索，不是对当前版本进行局部换色。

请重新审视：

- 首屏构图
- VDVXDV 标题比例
- 中文字体层级
- 文章与项目之间的视觉关系
- 色块和卡片的使用
- 首页整体节奏
- 留白
- 导航形态
- 移动端重排

必须保留：

- 当前信息架构
- 顶部导航栏目
- 文章视觉优先
- 项目入口优先
- 首页静态策展原则
- 桌面和移动端
- 真实中文示例内容

顶部导航可以尝试 Apple 式 Liquid Glass，但必须满足：

- 面积小
- 透明度克制
- 不影响文字清晰度
- 不扩散到主体内容
- 不使用复杂形变
- 静态状态已经成立

请不要只修改颜色。
需要形成一套与当前 v0.1 明显不同、但仍符合产品规范的完整视觉方案。

完成后请提供：

1. 桌面端完整首页
2. 移动端完整首页
3. 实际使用的 Design Tokens
4. 字体和字号层级
5. 布局与网格说明
6. 该方案如何减少 AI 模板感
7. 与当前 v0.1 的主要差异
8. 仍然需要人工判断的地方

不要继续生成其他页面。
```

---

# 七、并行操作规则

可以同时打开三个 Gemini 对话或 Canvas，但必须遵守：

1. 每个标签页只负责一个方案。
2. 标签页名称或对话标题必须清楚标记 A、B、C。
3. 不把某个方案的生成结果作为另一个方案的新参考。
4. 三个方案使用同一批原始资料。
5. 不在生成过程中互相复制代码或提示词结果。
6. 若浏览器或系统资源不足，改为依次运行，不要强行并行。
7. 若 Gemini 出现附件数量限制，采用与首轮相同的分批上传方式，但必须确认所有材料都进入对应对话。
8. 不上传无关 Google 文件。
9. 不打开 Gmail、Drive 或 Photos 中的无关私人内容。
10. 不允许 Codex 自行判断哪个方案最好并继续深化。

---

# 八、每套方案需要保存的材料

在项目目录建立：

```text
design/
├── gemini-home-v0.1/
├── gemini-home-a-editorial/
├── gemini-home-b-technical/
└── gemini-home-c-personal/
```

每个新方案目录包含：

```text
desktop.png
mobile.png
source/
GEMINI_NOTES.md
TOKENS.md
```

`GEMINI_NOTES.md` 写明：

- Gemini 对话名称
- 使用的模型
- 是否启用扩展思考
- 上传的文件清单
- 使用的专属设计方向
- Gemini 对参考网站的解释
- 是否出现越界内容
- 原型是否仍在 Canvas 中可访问

`TOKENS.md` 写明：

- 背景色
- 主文字色
- 辅助文字色
- 强调色
- 字体
- 字号层级
- 最大页面宽度
- 阅读宽度
- 间距
- 圆角
- 边框
- Liquid Glass 参数

---

# 九、最终对比报告

生成：

```text
design/GEMINI_HOME_COMPARISON.md
```

只做客观整理，不替用户决定。

至少包含以下表格：

| 项目 | v0.1 | A 编辑式 | B 技术感 | C 个人化 |
|---|---|---|---|---|
| 首屏构图 | | | | |
| AI 模板感 | | | | |
| 配色气质 | | | | |
| 文章视觉优先级 | | | | |
| 项目入口清晰度 | | | | |
| 作者辨识度 | | | | |
| 中文排版 | | | | |
| 卡片使用 | | | | |
| 导航 Liquid Glass | | | | |
| 移动端表现 | | | | |
| 工程可实现性 | | | | |

报告最后只写：

- 每套方案最明显的优点；
- 每套方案最明显的问题；
- 哪些部分可以混合；
- 哪些选择必须由用户亲自决定。

不要自动选出“最终方案”。

---

# 十、完成后停止

完成归档、三套方案和对比报告后：

- 不创建正式工程；
- 不安装依赖；
- 不开始技术架构调研；
- 不部署；
- 不把任何方案标记为最终版；
- 保持 Gemini Canvas 可访问；
- 向用户报告文件位置和当前状态；
- 等待用户进行视觉验收。
