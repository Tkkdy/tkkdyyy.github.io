# VDVXDV Personal Website — DESIGN.md

- 版本：v0.1
- 状态：初始设计规范，允许持续修订
- 依赖：`PRODUCT.md`、`CONTENT_MODEL.md`
- 使用对象：VDVXDV、Gemini、Codex，以及后续参与设计或开发的 AI
- 当前阶段：V0.1 静态视觉与基础页面搭建

---

# 0. 必读摘要

这是一个具有个人品牌感的技术创作者网站。

设计目标不是制造一个先锋艺术装置，也不是复制某一家科技公司或奢侈品牌，而是建立一个：

> 简洁、高级、严肃但温暖，现代且容易进入的个人互联网空间。

整体人格比例：

- 70% 开放、清晰、亲近、容易阅读；
- 30% 个人、先锋、具有辨识度。

V0.1 的首要任务：

1. 建立统一的字体、颜色、间距和网格；
2. 完成静态首页与各内容页面；
3. 保证文章、项目、随笔、碎片和影像清楚可读；
4. 保持电脑与手机端一致；
5. 避免明显的 AI 模板感；
6. 暂不加入特色动效。

最重要的设计原则：

- 内容优先，设计托住内容；
- 高级感来自比例、留白、字体和秩序，而不是堆特效；
- 可以使用卡片，但不能把所有东西都做成同一种卡片；
- 可以使用毛玻璃和 Liquid Glass，但 V0.1 只在少量界面层使用；
- 项目页可以更有科技公司和产品介绍感；
- 文章页必须安静、克制、适合长时间阅读；
- 所有页面最后必须看起来属于同一个 `VDVXDV` 网站。

---

# 1. 文档性质与修改原则

`DESIGN.md` 是一份持续演进的设计规范，不是永远不能修改的法律。

以后可以根据以下情况调整：

- Gemini 生成的真实页面效果；
- 手机与电脑端测试；
- 中文长标题和真实文章内容；
- 新看到的优秀网站；
- 新增内容类型或功能；
- 实际使用时发现的维护问题；
- V0.1 上线后的视觉复盘。

修改时应区分两类规则。

## 1.1 尽量保持稳定的规则

这些是网站的设计身份，不应频繁改变：

- 严肃但不冰冷；
- 简洁但不空洞；
- 文章阅读优先保证可读性；
- 首页是主动策展的静态封面；
- 项目在导航和操作入口上优先；
- 文章在首页视觉表达上优先；
- 暖白、近黑与低饱和色为主；
- 不公开真实姓名；
- 不复制参考网站；
- 不做高密度社区门户；
- 不做蓝紫霓虹 AI 模板；
- V0.1 不把特色动效作为重点。

## 1.2 可以通过原型继续调整的规则

这些应在真实页面中比较后决定：

- 最终字体；
- 精确色值；
- 首页各模块的具体大小；
- 卡片圆角；
- 图片比例；
- 首页首屏高度；
- 桌面端列数；
- 移动端导航形式；
- 某个板块是否使用深色背景；
- 某个内容类型的小图标；
- 强调色使用哪一组；
- 毛玻璃的透明度与模糊程度。

修改上述规则时，应同步更新本文件和前端设计变量。

---

# 2. 网站设计定位

## 2.1 核心定位

VDVXDV 是一个：

- 技术创作者个人主页；
- 项目和产品展示空间；
- 文章与技术博客；
- 随笔与碎片记录空间；
- 单张照片组成的影像流；
- 具有个人品牌辨识度的开放互联网空间。

它首先应该像一个成熟、清楚、容易使用的个人网站。

在此基础上，通过字体、配色、图像、排版和少量品牌细节表达个人审美。

## 2.2 希望访客产生的感受

### 刚进入时

- 页面很干净；
- 看起来高级、现代；
- 不需要学习如何使用；
- 很快知道网站属于谁；
- 很快知道项目、文章和其他内容在哪里。

### 浏览一段时间后

- 这个人有自己的审美；
- 文章和项目都经过选择；
- 设计很克制，但细节认真；
- 网站不是套用的普通博客模板；
- 技术感和人的温度同时存在。

### 离开以后

- 能记住 `VDVXDV`；
- 愿意再次回来；
- 能想起至少一篇文章或一个项目；
- 不会只记得某个炫技效果。

---

# 3. 参考网站分工

所有参考只用于学习设计方法，不得直接复制版式、插画、字体文件、品牌标识或受保护素材。

## 3.1 Anthropic 首页

学习：

- 暖白与近黑配色；
- 科技理性与人文温度；
- 粗壮现代的无衬线大标题；
- 衬线文字带来的编辑感；
- 大面积留白；
- 左右不完全对称的布局；
- 一个深色大区块制造页面节奏；
- 通过字号、字重和空间建立层级。

不学习：

- 企业宣传身份；
- 研究机构式品牌文案；
- 过低的信息密度；
- 直接复制其字体或品牌元素。

## 3.2 Anthropic Research

学习：

- 编辑部或研究机构式网格；
- 页面标题与简介左右分栏；
- 细分隔线；
- 多栏摘要；
- 大图与文章列表组合；
- 大小内容块并存；
- 信息较多但仍然有秩序。

适用于：

- 项目列表；
- 文章归档；
- 专题页面；
- 首页精选内容；
- 关于页面中的关注方向。

## 3.3 Aesop

学习：

- 不同内容板块使用不同低饱和色块；
- 通过色块、尺寸和卡片区分内容；
- 高级但不过分强烈的色彩对比；
- 不同区域拥有独立气氛；
- 卡片和大面积背景区块共同使用。

不学习：

- 电商商品结构；
- 促销式文案；
- 每个区域都做成产品陈列；
- 直接复制其商业品牌色。

## 3.4 Paco Coursey

学习：

- 极简个人主页骨架；
- Projects、Writing、Now 等清楚栏目；
- 多栏但不拥挤的布局；
- 短标题加一句说明；
- 下划线链接和细小排版细节；
- 快速说明作者正在做什么。

适用于：

- 首页内容骨架；
- 项目与文章入口；
- 关于与当前状态；
- 页脚与联系方式。

## 3.5 Lee Robinson

学习：

- 单栏自然叙述；
- 工程师兼作者的亲近感；
- 先介绍自己，再提供精选文章；
- 精选内容而不是机械展示最新内容；
- 不过度包装个人身份；
- 内容直接、真诚、容易阅读。

适用于：

- 首页介绍；
- 精选文章；
- 关于页面；
- 简单文字入口。

## 3.6 数字花园参考页面

学习：

- 柔和、原创感强的主题插画；
- 每篇 Note 前重复使用的小型品牌图标；
- 暖白背景与低饱和插画色；
- 随笔卡片与 Notes 列表的差异；
- 舒展的卡片内边距、行距和列距；
- 插画、文字与小图标形成统一内容系统。

适用于：

- 随笔页；
- 碎片页；
- 内容归档；
- 首页的个人化内容区域。

不学习：

- 直接复制植物图标；
- 直接复刻其插画；
- 将网站做成完全相同的数字花园。

## 3.7 少数派

学习：

- 首页作为内容总入口；
- 一项主内容配合少量辅助内容；
- 大、中、小内容模块建立优先级；
- 内容丰富但入口清楚；
- 文章、专题、项目等不同入口共存；
- 页面具有“可以继续逛”的感觉。

不学习：

- 高密度社区首页；
- 过多活动和商业模块；
- 大量鲜艳颜色；
- 过度活泼和热闹；
- 复杂侧栏；
- 平台式信息流焦虑。

转化原则：

> 保留少数派的信息组织能力，降低其活泼度、密度和商业感。

## 3.8 Vercel 风格页面

学习：

- 项目或产品首屏；
- 极简黑白；
- 几何品牌符号；
- 精确网格；
- 主次按钮；
- 技术栈和能力展示；
- 成熟科技公司的工程感。

适用于：

- 项目详情首屏；
- 技术专题；
- 技术文章的部分组件；
- 项目能力和架构区。

不学习：

- 商业 SaaS 营销话术；
- 虚假的合作方 Logo；
- 整站纯黑白造成的冷硬；
- 所有页面都变成产品落地页。

## 3.9 Apple 产品详情页

学习：

- 分章节讲述一个产品；
- 每个板块只突出一个信息；
- 大标题、大图、真实数据；
- 简洁但有视觉冲击力；
- 长页面仍然容易阅读；
- 不同章节拥有不同视觉主题；
- 后期组件随滚动自然出现。

适用于：

- 成熟项目详情；
- 项目发布页；
- 重点技术专题。

V0.1 限制：

- 先完成静态章节结构；
- 不实现滚动叙事；
- 不实现复杂动画；
- 不做购买页或商业营销结构。

## 3.10 文章详情参考页

学习：

- 较窄、居中的正文；
- 大面积留白；
- 正文、标题和图片是唯一主角；
- 初次进入显示全局导航；
- 下滑阅读时导航自动隐藏；
- 不使用复杂侧栏；
- 图片自然穿插在正文中。

---

# 4. 视觉人格

## 4.1 核心关键词

必须体现：

- 开放；
- 清晰；
- 克制；
- 温暖；
- 现代；
- 精确；
- 编辑感；
- 个人；
- 技术；
- 长期主义。

## 4.2 禁止关键词

避免：

- 赛博朋克；
- 蓝紫霓虹；
- 元宇宙；
- 游戏 HUD；
- 炫技；
- 发光边框；
- 满屏玻璃；
- 数据大屏；
- 商业模板；
- 奢华堆砌；
- 过度极简导致难以阅读；
- 过度可爱；
- 社区门户式热闹。

---

# 5. 颜色系统

以下颜色为 V0.1 原型的初始建议值，不是永远不能修改的最终答案。

Gemini 应先根据这组颜色生成真实页面，再由用户比较和调整。

## 5.1 基础颜色

```css
:root {
  --color-canvas: #F3F0E8;
  --color-canvas-soft: #F7F5EF;
  --color-surface: #FCFBF7;
  --color-surface-strong: #E9E3D8;

  --color-ink: #191918;
  --color-ink-soft: #46443F;
  --color-ink-muted: #77736B;
  --color-ink-inverse: #F6F3EC;

  --color-border: rgba(25, 25, 24, 0.14);
  --color-border-strong: rgba(25, 25, 24, 0.28);

  --color-dark-section: #191918;
  --color-dark-section-soft: #242320;
}
```

视觉含义：

- `canvas`：暖白、米白的全站基础背景；
- `surface`：比背景更浅的内容表面；
- `ink`：近黑主文字；
- `ink-soft`：正文或辅助说明；
- `ink-muted`：日期、标签和元数据；
- `dark-section`：首页或项目页中的重点深色章节。

不要默认使用纯白 `#FFFFFF` 和纯黑 `#000000` 作为大面积主体。

## 5.2 低饱和强调色

V0.1 最多选择一个主强调色，并允许少量辅助色只出现在插画和影像中。

候选方向：

```css
--color-accent-wine: #7A3F46;
--color-accent-blue: #4A6A72;
--color-accent-ochre: #9B7041;
--color-accent-green: #5D6B58;
```

使用范围：

- 当前导航项；
- 内容类型小图标；
- 链接悬停；
- 标签；
- 项目状态；
- 少量重点文字；
- 插画局部。

限制：

- 同一屏不要同时强调三四种品牌色；
- 强调色不用于大面积高饱和渐变；
- 不以颜色作为唯一状态提示；
- 不让项目状态色破坏整体克制感。

## 5.3 Aesop 式分区色块

页面板块可以使用以下类型的背景：

- 暖白；
- 浅砂色；
- 浅灰褐；
- 低饱和蓝灰；
- 低饱和酒红；
- 近黑。

每个页面建议只有一到两个明显色块变化。

不同色块之间应通过大面积区域切换，而不是把每一张小卡片染成不同颜色。

## 5.4 深色模式

V0.1 不要求完整深色模式。

第一版可以先确保：

- 浅色主题完成；
- 深色章节可读；
- 系统后续容易加入深色模式。

如果实现深色模式，不能只是把颜色机械反转。

---

# 6. 字体系统

字体关系比具体字体名称更重要。

## 6.1 字体角色

### 界面无衬线字体

用于：

- `VDVXDV`；
- 导航；
- 页面大标题；
- 项目名称；
- 按钮；
- 标签；
- 日期；
- 数据；
- 技术组件。

气质：

- 现代；
- 清晰；
- 精确；
- 有技术感。

### 编辑型衬线字体

用于：

- 文章和随笔标题；
- 长文正文；
- 引用；
- 首页重要宣言；
- 较具人文感的说明文字。

气质：

- 书籍；
- 编辑；
- 人文；
- 有温度；
- 适合阅读。

### 等宽字体

用于：

- 代码；
- 命令；
- 技术标识；
- 少量项目状态；
- 版本号。

## 6.2 初始字体栈

第一版优先使用稳定、合法、可维护的字体栈。

```css
--font-interface:
  Inter,
  "SF Pro Display",
  "SF Pro Text",
  "Segoe UI",
  "PingFang SC",
  "Microsoft YaHei",
  system-ui,
  sans-serif;

--font-editorial:
  "Source Han Serif SC",
  "Noto Serif CJK SC",
  "Songti SC",
  SimSun,
  serif;

--font-code:
  "SFMono-Regular",
  "Cascadia Code",
  "JetBrains Mono",
  Consolas,
  monospace;
```

说明：

- 不应擅自分发受限制的 Apple 字体文件；
- 后续若自托管开源中文字体，应评估文件体积；
- Gemini 原型阶段可以先使用接近气质的字体；
- Codex 工程化阶段必须检查授权、性能和中文显示。

## 6.3 字号层级建议

```css
--text-xs: 0.75rem;
--text-sm: 0.875rem;
--text-base: 1rem;
--text-lg: 1.125rem;
--text-xl: 1.375rem;
--text-2xl: 1.75rem;
--text-3xl: 2.25rem;
--text-4xl: 3rem;
--text-5xl: clamp(3.5rem, 8vw, 7rem);
```

规则：

- `VDVXDV` 和首屏标题可以使用超大字号；
- 长中文标题必须允许自动换行；
- 不使用过小、过淡的正文制造高级感；
- 普通正文桌面端通常不小于 16px；
- 文章正文建议为 17–19px；
- 移动端正文不小于 16px。

## 6.4 行高

建议：

- 页面大标题：1.0–1.1；
- 栏目标题：1.15–1.25；
- 普通说明：1.5–1.7；
- 文章正文：1.75–1.95；
- 元数据：1.4–1.6。

中英文混排必须实际测试，不能只看英文效果。

---

# 7. 网格与内容宽度

## 7.1 桌面端

建议：

```css
--page-max-width: 1440px;
--content-max-width: 1200px;
--content-narrow-width: 960px;
--reading-width: 720px;
```

结构：

- 页面最大宽度不无限扩张；
- 大屏保持内容居中；
- 首页与项目页可以使用 12 列网格；
- 文章详情使用独立窄阅读列；
- 影像页可根据照片比例突破普通内容宽度。

## 7.2 移动端

使用 4 列网格或单栏布局。

规则：

- 不把桌面三栏机械压缩成三列；
- 主要内容按优先级重新排序；
- 触摸区域至少约 44px；
- 横向滚动只用于确实需要的影像或代码区域；
- 导航必须容易打开和关闭。

## 7.3 页面边距

建议：

```css
--page-padding-mobile: 20px;
--page-padding-tablet: 32px;
--page-padding-desktop: 48px;
--page-padding-wide: 64px;
```

## 7.4 文章阅读宽度

文章正文目标宽度约为：

```css
max-width: 42rem;
```

图片可以分为：

- 正文同宽；
- 中等宽度；
- 全宽突破阅读列。

不能让正文一行过长。

---

# 8. 间距系统

统一使用以下基础间距：

```css
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 24px;
--space-6: 32px;
--space-7: 48px;
--space-8: 64px;
--space-9: 96px;
--space-10: 128px;
```

## 8.1 使用原则

- 卡片内部通常使用 24–40px；
- 大板块之间通常使用 64–128px；
- 标题与说明之间通常使用 16–32px；
- 内容列表之间不应挤在一起；
- 手机端缩小间距，但保持节奏；
- 不同页面必须共享同一套尺度。

## 8.2 高级感来源

高级感不能通过“什么都做得很小”获得。

高级感来自：

- 足够的空间；
- 稳定的对齐；
- 少量明确的重点；
- 合理的字体比例；
- 图片与文字之间的呼吸感。

---

# 9. 圆角、边框与阴影

## 9.1 圆角

建议：

```css
--radius-sm: 8px;
--radius-md: 16px;
--radius-lg: 24px;
--radius-pill: 999px;
```

规则：

- 按钮可使用小圆角或胶囊圆角；
- 大色块可使用中到大圆角；
- 文章正文不需要套圆角卡片；
- 列表项可以只使用分隔线；
- 不同内容不能全部使用相同的 24px 圆角卡片。

## 9.2 边框

优先使用：

- 细分隔线；
- 低透明边框；
- 背景色差；
- 空间分组。

不要大量使用粗边框。

## 9.3 阴影

V0.1 阴影必须克制。

建议：

- 普通卡片无阴影或极弱阴影；
- 浮层和搜索面板可以有轻微阴影；
- 不使用大面积发光阴影；
- 不依赖阴影区分所有内容。

---

# 10. 毛玻璃与 Liquid Glass

## 10.1 V0.1 使用范围

允许用于：

- 顶部导航；
- 搜索面板；
- 移动端菜单；
- 图片查看器控制层；
- 少量悬浮工具；
- 未来评论输入浮层。

不建议用于：

- 文章正文；
- 每一张项目卡片；
- 整个页面背景；
- 大量内容列表；
- 所有按钮。

## 10.2 基础玻璃样式

```css
background: rgba(247, 245, 239, 0.72);
backdrop-filter: blur(18px) saturate(120%);
border: 1px solid rgba(25, 25, 24, 0.10);
```

实际数值需要在真实背景和手机性能中测试。

## 10.3 Liquid Glass

V0.1 不实现复杂形变、折射或跟随动画。

第一版只保留设计接口，后续再决定：

- 哪个按钮；
- 哪个导航状态；
- 哪个图片查看器；
- 哪个项目展示区；

值得加入更强的 Liquid Glass 表现。

---

# 11. 图像、摄影与插画

## 11.1 图片原则

- 优先使用用户自己的摄影；
- 图片应该成为内容，而不是装饰背景；
- 保持原始比例；
- 不强制所有照片裁成统一卡片；
- 需要裁切时保留视觉主体；
- 提供清晰的替代文字；
- 清理不必要的 EXIF 隐私数据。

## 11.2 随笔配图

随笔允许：

- 一张主图；
- 多张不规则照片；
- 横图与竖图混排；
- 没有直接解释关系的气氛照片；
- 图片突破正文宽度；
- 留有较大空白。

图片不能把随笔强制变成相册。

## 11.3 影像页面

目标接近单张摄影作品流：

- 一条内容对应一张主要照片；
- 图片优先；
- 信息最少化；
- 保留照片比例；
- 不使用密集商品瀑布流；
- 点击后可进入更沉浸的单张查看；
- 未来可加入点赞、评论和切换。

## 11.4 插画

插画应：

- 柔和；
- 低饱和；
- 与内容主题有关；
- 有手绘感或个人气质；
- 不抢过文字；
- 风格统一。

禁止：

- 每篇文章使用完全不同的 AI 插画风格；
- 明显图库素材感；
- 直接复制参考网站插画；
- 以装饰性 3D 图标填满页面。

## 11.5 内容类型图标

可以为以下内容设计小型符号：

- 文章；
- 随笔；
- 碎片；
- 项目；
- 影像。

第一版最多先使用一到两种有明确价值的符号。

图标应：

- 简单；
- 容易重复；
- 与 VDVXDV 品牌有关；
- 在小尺寸仍然清楚；
- 不直接复制数字花园页面的植物图标。

---

# 12. 导航系统

## 12.1 桌面端导航

顺序：

`VDVXDV｜项目｜随笔｜文章｜碎片｜影像｜关于｜搜索`

规则：

- `VDVXDV` 位于左侧；
- 点击返回首页；
- 项目是右侧第一个内容入口；
- 当前页面状态清楚但克制；
- 导航高度较低；
- 文字尺寸不能过小；
- 搜索使用文字或易懂图标；
- 初始状态可以采用轻微毛玻璃。

## 12.2 自动隐藏

在文章详情页：

- 初次进入正常显示导航；
- 向下阅读时自动隐藏；
- 向上滚动时可再次出现；
- 键盘聚焦导航时不得突然隐藏；
- 关闭 JavaScript 时页面仍可使用。

其他页面 V0.1 不强制自动隐藏。

## 12.3 移动端导航

可使用：

- 顶部折叠菜单；
- 简洁菜单面板；
- 后续再测试底部 Liquid Glass 导航。

V0.1 优先选择更稳定、容易理解的顶部折叠菜单。

---

# 13. 按钮与链接

## 13.1 按钮层级

### 主要按钮

- 近黑背景；
- 暖白文字；
- 用于最重要行动；
- 一个区域通常只有一个。

### 次要按钮

- 透明或浅色背景；
- 细边框；
- 用于 GitHub、查看更多等次级操作。

### 文字链接

- 用于文章、导航和轻量跳转；
- 可以使用下划线、箭头或颜色变化；
- 不需要全部做成按钮。

## 13.2 按钮文案

使用直接、自然的中文：

- 查看项目；
- 阅读文章；
- 查看源代码；
- 在线体验；
- 查看全部；
- 继续阅读。

避免：

- 立即解锁；
- 开启未来；
- 颠覆体验；
- 一键赋能；
- 其他明显 AI 或 SaaS 营销话术。

---

# 14. 组件设计规则

## 14.1 组件化原则

只有在多个位置重复出现，并拥有相同意义和行为时，才建立通用组件。

不要为了“代码看起来高级”把所有区域抽象成同一个万能卡片。

## 14.2 必要基础组件

V0.1 可以包含：

- `SiteHeader`
- `MobileNavigation`
- `SiteFooter`
- `PageContainer`
- `SectionHeading`
- `ArticlePreview`
- `EssayPreview`
- `FragmentItem`
- `ProjectPreview`
- `ImageItem`
- `Tag`
- `StatusBadge`
- `PrimaryButton`
- `SecondaryButton`
- `TextLink`
- `SearchDialog`
- `Prose`
- `CodeBlock`
- `Figure`

## 14.3 内容组件差异

### ArticlePreview

- 标题最突出；
- 有摘要、日期和阅读时间；
- 可以有封面；
- 无封面时转为纯排版；
- 首页版本可以明显放大。

### EssayPreview

- 更轻；
- 可使用插画或照片；
- 摘要较短；
- 更有人文气质；
- 栏目精选可以使用较大视觉卡片。

### FragmentItem

- 不要求标题；
- 使用时间、正文和可选图标；
- 可使用细分隔线；
- 避免每条都套厚重卡片。

### ProjectPreview

- 一句话说明必须清楚；
- 显示项目状态；
- 技术栈不能抢过项目目的；
- 项目封面可有可无；
- 重点项目可以使用大色块或深色区域。

### ImageItem

- 图片是主角；
- 元数据极少；
- 保留比例；
- 不使用统一商品卡片式裁切。

---

# 15. 首页设计规范

## 15.1 首页职责

首页是主动策展的静态封面。

不是：

- 自动最新文章列表；
- 所有项目目录；
- 社区门户；
- 无限信息流；
- 动画展示页。

## 15.2 首页结构建议

### 第一部分：身份封面

包含：

- `VDVXDV`；
- 一句简洁定位；
- 项目与文章的清楚入口；
- 一项代表性视觉内容。

要求：

- 第一眼能理解；
- 不使用晦涩宣言；
- 不需要复杂动态；
- 高度控制在约一屏以内；
- 不连续滚动几屏仍只有一句话。

### 第二部分：主精选区

采用“文章视觉优先、项目操作优先”原则。

桌面端可以：

- 左侧或主要区域：代表文章；
- 右侧或辅助区域：重点项目；
- 小区域：一篇随笔、一条碎片或当前状态。

移动端顺序建议：

1. 代表文章；
2. 重点项目；
3. 随笔或碎片。

### 第三部分：少量精选项目

- 1–3 个；
- 不展示所有项目；
- 每个项目说明目的和状态；
- 提供查看全部入口。

### 第四部分：随笔或碎片

- 使用与文章不同的轻量形式；
- 随笔可带照片；
- 碎片可使用列表和小图标；
- 数量受控。

### 第五部分：影像

- 1–4 张精选照片；
- 不做复杂相册轮播；
- 图片大小可以不同；
- 点击进入影像页或单张详情。

### 第六部分：关于与页脚

- 简短介绍；
- GitHub、邮箱等公开入口；
- 不公开真实姓名。

## 15.3 首页板块长度

首页应保持在大约 2–4 个桌面屏幕长度内。

可以根据真实内容调整，但不应成为无尽信息流。

## 15.4 AI 首页建议

设计上可以预留：

- 首页内容最后更新时间；
- 策展配置；
- AI 建议的内部预览。

线上页面不显示 AI 管理界面。

---

# 16. 项目列表页

## 16.1 气质

- 比首页更有工程感；
- 保留全站暖白和人文气质；
- 可使用 Anthropic Research 式网格；
- 可使用细分隔线和状态标签；
- 不做商业产品商店。

## 16.2 信息层级

每个项目应首先回答：

1. 它是什么；
2. 为什么存在；
3. 当前状态；
4. 是否可以体验；
5. 是否开源；
6. 最近更新时间。

技术栈是补充，不是主标题。

## 16.3 布局

可以组合：

- 一个大型重点项目；
- 其余项目多栏网格；
- 按状态分组；
- 按更新时间排序；
- 使用细线和低饱和色区分。

---

# 17. 项目详情页

## 17.1 参考分工

- Vercel：首屏、工程精度、技术栈、按钮；
- Apple：章节结构、重点信息、数据与未来动效；
- Anthropic：配色、字体和温度；
- 全站系统：导航、页脚、间距和文字层级。

## 17.2 静态 V0.1 结构

建议章节：

1. 项目名称与一句话定位；
2. 在线体验和 GitHub；
3. 大幅界面截图或项目视觉；
4. 为什么做；
5. 核心能力；
6. 使用方式或产品流程；
7. 技术架构；
8. 真实数据和状态；
9. 开发过程；
10. 当前限制；
11. 更新日志；
12. 后续计划。

每个章节只承担一个主要任务。

## 17.3 首屏

可采用三段式：

- 左：名称、定位、按钮；
- 中：项目符号、截图或主视觉；
- 右：三项核心能力或状态。

移动端改为自然单栏。

## 17.4 数据展示

数据必须真实。

可以展示：

- 版本；
- 信息源数量；
- 测试数量；
- 更新日期；
- 支持平台；
- 运行频率；
- 项目阶段；
- 前后版本变化。

不得制造虚假指标。

## 17.5 后续动效

V0.1 完成后再评估：

- 图片展开；
- 架构层逐步出现；
- 数据进入视野；
- 前后版本切换；
- 滚动章节过渡。

动效必须帮助理解，不能随机出现。

---

# 18. 文章列表页

## 18.1 目标

- 清楚；
- 安静；
- 适合浏览；
- 技术文章和其他长文均可存在；
- 不做新闻门户。

## 18.2 推荐结构

- 页面标题与简介左右分栏；
- 一篇或少量精选文章；
- 完整文章列表；
- 日期、标签和阅读时间；
- 可选分类与搜索；
- 使用分隔线或编辑网格；
- 卡片数量受控。

## 18.3 技术博客气质

技术文章区域可以增加：

- 更精确的无衬线标题；
- 等宽标签；
- 代码或架构缩略图；
- 黑白对比；
- 简单几何元素。

但不能因此脱离全站暖白、亲近的气质。

---

# 19. 文章详情页

## 19.1 阅读区

- 正文居中；
- 最大宽度约 42rem；
- 大面积留白；
- 首屏包含标题、摘要、日期、阅读时间和标签；
- 正文使用编辑型衬线或经过验证的阅读字体；
- 技术代码使用等宽字体。

## 19.2 导航行为

- 初次进入显示导航；
- 向下阅读后自动隐藏；
- 向上滚动重新出现；
- 不影响键盘使用；
- 不遮挡标题和目录。

## 19.3 正文元素

必须设计：

- 一级到三级标题；
- 段落；
- 链接；
- 引用；
- 有序和无序列表；
- 图片和图注；
- 代码块；
- 行内代码；
- 表格；
- 提示框；
- 分隔线。

## 19.4 图片

支持：

- 正文同宽；
- 中等突破；
- 全宽大图；
- 横图和竖图；
- 多张图片之间的大留白。

## 19.5 目录

V0.1 可以：

- 长技术文章显示目录；
- 普通文章不强制目录；
- 移动端目录折叠；
- 不建立始终占据屏幕的大侧栏。

## 19.6 文末

可以包含：

- 标签；
- 发布时间和更新时间；
- 上一篇、下一篇；
- 返回文章列表；
- 未来评论与反应。

---

# 20. 随笔页与随笔详情

## 20.1 随笔列表

可以使用：

- 较大视觉卡片；
- 柔和照片或插画；
- 标题和极短摘要；
- 一部分精选；
- 其余按时间排列。

可以参考数字花园页面的：

- Essays 大卡片；
- Notes 轻列表；
- 不对称双栏。

## 20.2 随笔详情

- 比文章更轻；
- 正文可以更宽松；
- 不强制目录；
- 图片可以不规则穿插；
- 标题、文字和照片之间保留较大间距；
- 不把照片解释得过度正式。

---

# 21. 碎片页

## 21.1 布局

- 时间流；
- 轻量文字列表；
- 可选小型品牌图标；
- 细分隔线；
- 不要求标题；
- 不使用大面积封面；
- 每条之间留有足够空间。

## 21.2 气质

- 比文章和随笔更即时；
- 不显得像社交媒体；
- 不显示夸张互动数字；
- 不模仿微博或 X；
- 保持个人记录感。

---

# 22. 影像页

## 22.1 核心目标

像单张摄影作品流，而不是相册目录或商品瀑布流。

## 22.2 布局建议

可测试：

- 单列大图；
- 两列但保持原比例；
- 大小交错的编辑式网格；
- 一张重点图配少量辅助图。

规则：

- 照片不能被统一裁成同样比例；
- 页面背景和文字尽量安静；
- 标题、日期和地点只是补充；
- 手机端优先单列；
- 不使用过密 masonry 瀑布流。

## 22.3 单张查看

未来可以包括：

- 沉浸式大图；
- 拍摄时间；
- 可选地点；
- 简短说明；
- 前后切换；
- 点赞和评论。

V0.1 可以先完成静态详情和基本图片查看。

---

# 23. 关于页

## 23.1 内容

- 使用网络名称 `VDVXDV`；
- 说明这个网站是什么；
- 说明关注方向；
- 说明项目、文章与影像的关系；
- 展示公开联系方式；
- 不公开真实姓名和不必要隐私。

## 23.2 设计

可以结合：

- Lee Robinson 的自然叙述；
- Anthropic 的左右分栏；
- Paco 的简洁栏目；
- 一张个人选择的影像；
- 少量关注方向或状态。

避免：

- 技能进度条；
- 大量技术 Logo；
- 传统简历时间线；
- 夸张自我宣传。

---

# 24. 搜索页

## 24.1 目标

简单、直接、快速。

## 24.2 设计

- 显眼搜索框；
- 支持键盘操作；
- 结果按内容类型标识；
- 标题、摘要和日期清楚；
- 无结果时给出诚实提示；
- 不使用复杂推荐算法；
- 搜索浮层可以使用轻微玻璃效果。

---

# 25. 响应式设计

## 25.1 断点建议

```css
--breakpoint-sm: 640px;
--breakpoint-md: 768px;
--breakpoint-lg: 1024px;
--breakpoint-xl: 1280px;
```

断点不是为了特定设备，而是根据布局何时失效来调整。

## 25.2 必测尺寸

至少测试：

- 360px 手机；
- 390px 手机；
- 768px 平板；
- 1366px 笔记本；
- 1920px 桌面显示器。

## 25.3 重排原则

- 桌面双栏或三栏在手机上改为单栏；
- 顺序依据内容优先级重新排列；
- 不隐藏重要内容；
- 不把文字缩到难以阅读；
- 图片保持比例；
- 表格与代码允许局部横向滚动；
- 按钮和链接适合触摸。

---

# 26. 可访问性与可读性

V0.1 必须考虑：

- 正文颜色对比；
- 键盘导航；
- 可见焦点状态；
- 图片替代文字；
- 导航语义；
- 正确标题层级；
- 减少动态偏好；
- 屏幕阅读器标签；
- 触摸区域；
- 200% 页面缩放；
- 中文断行；
- 英文长链接；
- 无 JavaScript 的基础可用性。

不能为了“高级感”使用：

- 太淡的灰色正文；
- 过小字体；
- 只靠颜色表示状态；
- 无焦点状态的按钮；
- 无说明的纯图标。

---

# 27. V0.1 动效边界

## 27.1 允许

只允许基础反馈：

- 链接颜色变化；
- 按钮按下反馈；
- 卡片极轻微悬停；
- 菜单打开和关闭；
- 图片查看器淡入；
- 导航自动隐藏与重新出现；
- 页面系统默认平滑滚动；
- 尊重 `prefers-reduced-motion`。

## 27.2 暂不实现

- Apple 式滚动叙事；
- 液态玻璃形变；
- 鼠标跟随光效；
- 粒子背景；
- 3D 旋转；
- 大量卡片逐个弹出；
- 视差；
- 背景持续漂浮；
- 为每个区块添加进入动画。

## 27.3 后续原则

未来每个页面最多选择一个主要记忆点动效。

其余都应是轻量微交互。

---

# 28. 禁止的设计方向

Gemini 和 Codex 不得默认加入：

1. 蓝紫渐变 AI 背景；
2. 发光描边卡片；
3. 满屏透明玻璃；
4. 每个模块完全相同的圆角卡片；
5. 三张功能卡片加三个图标的通用 SaaS 布局；
6. 无意义的 3D 球体和几何漂浮物；
7. “探索未来”“开启无限可能”等空洞文案；
8. 过小、过淡的正文；
9. 高密度信息门户；
10. 过于活泼的社区首页；
11. 游戏界面或数据大屏风格；
12. 每一屏都使用不同设计语言；
13. 直接复制参考网站；
14. 使用未经授权的字体和素材；
15. 使用企业 Logo 暗示不存在的合作；
16. 让项目技术栈压过项目目的；
17. 自动生成虚假的数据；
18. 为了动画牺牲加载速度和可访问性；
19. 公开真实姓名或隐私元数据；
20. 把 V0.1 做成难以维护的大型前端工程。

---

# 29. Gemini 原型生成规则

## 29.1 第一轮输入

提供：

- `PRODUCT.md`
- `CONTENT_MODEL.md`
- `DESIGN.md`
- 首页相关参考截图
- 少量真实内容

第一轮只生成：

- 首页桌面版；
- 首页手机版；
- 基础导航；
- 基础页脚。

不要同时生成整个网站。

## 29.2 第一轮要求

Gemini 必须：

- 使用真实中文内容；
- 保持首页静态；
- 不加入特色动效；
- 不直接复制参考截图；
- 说明每个参考来源被转化成了什么；
- 使用统一设计变量；
- 避免绝对定位堆叠页面；
- 保证手机版重新排版；
- 不自行增加产品范围。

## 29.3 第二轮

首页通过后生成：

- 文章详情页；
- 项目详情页。

Apple 与 Vercel 参考只在项目详情页阶段重点加入。

## 29.4 第三轮

再生成：

- 文章列表；
- 随笔；
- 碎片；
- 影像；
- 关于；
- 搜索。

---

# 30. Codex 工程化规则

Codex 应：

- 以这三份文档为产品和设计依据；
- 检查 Gemini 代码，不默认原样沿用；
- 建立统一设计 Tokens；
- 建立可复用但不过度抽象的组件；
- 使用内容集合驱动页面；
- 保证 GitHub Pages 路径正确；
- 保证响应式；
- 加入基础测试；
- 检查无障碍和性能；
- 不擅自增加复杂依赖；
- 不在 V0.1 实现特色动效；
- 不擅自修改内容类型边界；
- 每次只完成一个可验收模块。

如果 Gemini 原型与文档冲突，以用户最新确认和文档为准。

---

# 31. 视觉验收清单

每次原型或实现提交后，逐项检查。

## 31.1 整体

- [ ] 一眼看起来简洁、现代、具有设计感
- [ ] 高级但不疏远
- [ ] 严肃但仍有温暖
- [ ] 不像通用 AI 模板
- [ ] 不像商业 SaaS 官网
- [ ] 各页面属于同一个网站
- [ ] `VDVXDV` 身份清楚
- [ ] 没有真实姓名

## 31.2 首页

- [ ] 首页是策展封面，不是最新内容流
- [ ] 文章获得最强视觉表达
- [ ] 项目入口最容易找到
- [ ] 展示内容数量受控
- [ ] 能快速理解这里有什么
- [ ] 不需要依靠动效成立
- [ ] 没有少数派式过密信息

## 31.3 字体与配色

- [ ] 暖白与近黑关系自然
- [ ] 强调色克制
- [ ] 中文字体清楚
- [ ] 无衬线与衬线分工明确
- [ ] 正文不太小、不太淡
- [ ] 中英文混排协调

## 31.4 网格与间距

- [ ] 页面边界统一
- [ ] 大板块之间距离舒展
- [ ] 卡片内边距统一
- [ ] 对齐稳定
- [ ] 不同内容尺寸有清楚层级
- [ ] 大屏不会空散
- [ ] 手机不会拥挤

## 31.5 内容类型

- [ ] 文章看起来正式、适合长读
- [ ] 随笔更轻并允许不规则照片
- [ ] 碎片不需要标题
- [ ] 项目目的先于技术栈
- [ ] 影像以单张照片为主
- [ ] 无封面时布局仍然完整

## 31.6 文章页

- [ ] 正文宽度舒适
- [ ] 导航初始可见
- [ ] 向下阅读可自动隐藏
- [ ] 图片与文字之间有足够空间
- [ ] 代码和表格可读
- [ ] 没有复杂侧栏干扰

## 31.7 项目页

- [ ] 首屏清楚说明项目
- [ ] 在线体验与 GitHub 入口明确
- [ ] 每个章节只讲一个重点
- [ ] 数据真实
- [ ] 工程感不等于商业营销
- [ ] 静态页面本身已经成立

## 31.8 响应式与可访问性

- [ ] 360px 手机可用
- [ ] 1366px 笔记本可用
- [ ] 1920px 显示器不失控
- [ ] 键盘可操作
- [ ] 焦点可见
- [ ] 图片有替代文字
- [ ] 200% 缩放可读
- [ ] 支持减少动态偏好

---

# 32. V0.1 设计完成标准

`DESIGN.md` 进入下一阶段时，应满足：

1. Gemini 可以根据文档生成首页原型，而不需要自行猜测网站风格；
2. Gemini 知道每个参考网站分别贡献什么；
3. 首页、文章页和项目页有不同职责，但共享同一设计语言；
4. 颜色、字体、间距、圆角和宽度拥有初始规则；
5. 所有规则允许在真实原型中继续调整；
6. V0.1 不依赖特色动效；
7. 手机端不是桌面端的机械缩小；
8. 内容缺少封面、图片或链接时仍然成立；
9. Codex 可以把规范转化为统一 CSS Tokens 和组件；
10. 最终页面呈现简洁、高级、温暖、严肃、现代且具有 VDVXDV 的个人辨识度。
